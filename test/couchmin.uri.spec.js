'use strict';


const Assert = require('assert');
const Exec = require('./exec');


describe('couchmin uri', () => {

  it.skip('should ...', (done) => {

    Exec(['uri'], (err, stdout, stderr) => {

      Assert.ok(!err);
      done();
    });
  });

});

